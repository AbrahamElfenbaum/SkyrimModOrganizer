#include "Functions.h"
int main()
{
   /*
   Questions:
   What happens when a mod is not on Nexus? (SKSE)
   */
	AddMod();
	//ShowAllCategories();
	/*
	std::string name;
	std::cout << "Type: ";
	getline(std::cin, name);

	while (name.empty())
	{
		std::cout << "Error. Type: ";
		getline(std::cin, name);
	}
	*/
}